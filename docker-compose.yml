version: "3.8"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: scheduler_dev
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
